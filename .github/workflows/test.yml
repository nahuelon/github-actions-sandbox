name: test

on:
  pull_request:
    branches:
      - main

jobs:
  test:
    name: test
    runs-on: ubuntu-latest
    steps:
      - run: echo "::error file=app.js,line=1::Missing semicolon"
      - run: "echo \"::notice title=🎭 Playwright Run Summary::  3 failed%0A    [chromium] › example.spec.ts:3:5 › has title ───────────────────────────────────────────────────%0A    [firefox] › example.spec.ts:3:5 › has title ────────────────────────────────────────────────────%0A    [webkit] › example.spec.ts:3:5 › has title ─────────────────────────────────────────────────────%0A  3 passed (8.8s)\"
